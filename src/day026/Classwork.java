/*
package day026;

public class Classwork {

    */
/**
     * Given a string and a non-empty word string,
     * return a string made of each char just before and just after every appearance of the word in the string.
     * <p>
     * Ignore cases where there is no char before or after the word, and a char may be included twice if it is between two words.
     * *
     * Expectation:
     * wordEnds("abcXY123XYijk", "XY") → "c1   3i"
     * wordEnds("XY123XY", "XY") → "13"
     * wordEnds("XY1XY", "XY") → "11"
     * *
     * Test:
     *//*


    public static void main(String[] args) {


*/
/*        String result = "";
        String pattern = "bread";

        int firstIndex = input.indexOf(pattern);
        int lastIndex = input.lastIndexOf(pattern);*//*



        String word = "XY";
        String myString = "abcXY123XYijk";

        wordEnds(myString, word);
    }

    private static String wordEnds(String myString, String word) {

        for (int k = 0, k < myString.length(); k++){

           // myString.indexOf(word);

            System.out.println("here");

        }


        return "OUTPUT";
    }
}
*/
